import React from 'react';
import Particles from 'react-particles-js';

const ParticleAnimation = ({ canvasClassName, params }) => {
  return <Particles canvasClassName={canvasClassName} params={params} />;
};

export default ParticleAnimation;
